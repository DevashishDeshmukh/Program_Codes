#include <stdio.h>
int main()
{ float r, A, C;
    printf("Enter radius of circle = ");
    scanf("%f", &r);
    printf("radius of circle given by you is %f",r);
    printf("\nArea of circle(A) =  %f",A = 3.14 * r * r);
    printf("\nCircumference of circle(C) = %f", C = 2 * 3.14 * r);
    system("pause");
    return 0;
}

